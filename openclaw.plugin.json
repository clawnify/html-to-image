{
  "id": "html-to-image",
  "name": "HTML to Image",
  "description": "Render an HTML string to a PNG file on disk (Satori + resvg, no headless browser). Registers the html_to_image tool.",
  "version": "0.3.0",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": ["html_to_image"]
  },
  "skills": ["./skills/html-to-image"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
